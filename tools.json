{
  "tools": [
    {
      "name": "api-health",
      "description": "Checks the health and availability of Veracode API endpoints. Call this first if API connectivity is uncertain before running scans or retrieving findings.",
      "category": "veracode",
      "params": []
    },
    {
      "name": "dynamic-findings",
      "description": "Get runtime vulnerabilities from platform-based DYNAMIC analysis scans (auth/session flaws, authorization bypasses, CSRF, clickjacking, etc.). These are findings from scans already completed on the Veracode platform — this tool does not run a new scan. Do not use for local or pipeline scan results.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "app_profile",
          "type": "string",
          "isRequired": false,
          "description": "Application name (auto-detected if not provided)"
        },
        {
          "name": "severity",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Exact severity (0=Info, 1=VeryLow, 2=Low, 3=Med, 4=High, 5=VeryHigh)"
        },
        {
          "name": "severity_gte",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Minimum severity (0-5)"
        },
        {
          "name": "cwe_ids",
          "type": "array",
          "isRequired": false,
          "description": "Filter by CWE IDs (e.g., [89, 79] for SQLi and XSS)"
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        },
        {
          "name": "violates_policy",
          "type": "boolean",
          "isRequired": false,
          "description": "Filter by policy violation status. Default is true (policy violations only). Only set this parameter if the user explicitly asks to see all findings regardless of policy. Do not set it to false unless directly requested."
        }
      ]
    },
    {
      "name": "static-findings",
      "description": "Get source code vulnerabilities from platform-based STATIC analysis scans on the Veracode platform (XSS, SQLi, crypto issues, buffer overflows, etc.). These are findings from scans already completed on the platform — this tool does not run a new scan. Do not use for local pipeline scan results; use pipeline-findings for those.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "app_profile",
          "type": "string",
          "isRequired": false,
          "description": "Application name (auto-detected if not provided)"
        },
        {
          "name": "severity",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Exact severity (0=Info, 1=VeryLow, 2=Low, 3=Med, 4=High, 5=VeryHigh)"
        },        
        {
          "name": "severity_gte",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Minimum severity (0-5)"
        },
        {
          "name": "cwe_ids",
          "type": "array",
          "isRequired": false,
          "description": "Filter by CWE IDs (e.g., [89, 79] for SQLi and XSS)"
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        },
        {
          "name": "violates_policy",
          "type": "boolean",
          "isRequired": false,
          "description": "Filter by policy violation status. Default is true (policy violations only). Only set this parameter if the user explicitly asks to see all findings regardless of policy. Do not set it to false unless directly requested."
        }
      ]
    },
    {
      "name": "finding-details",
      "description": "Get detailed information about a specific flaw including full data-flow paths (SAST) or request/response info (DAST). Determines scan type automatically from flaw ID format: plain numeric (e.g., '12345') indicates a platform scan flaw; numeric with occurrence suffix (e.g., '12345-1') indicates a pipeline scan flaw. Use selectively for findings where the full data-flow path would materially change the remediation approach — not for every finding.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "app_profile",
          "type": "string",
          "isRequired": false,
          "description": "Application name (auto-detected if not provided). Only used for platform scans."
        },
        {
          "name": "flaw_id",
          "type": "string",
          "isRequired": true,
          "description": "Flaw ID to retrieve. Format: '1234' for platform scans, '1234-1' for pipeline scans with occurrence number."
        }
      ]
    },
    {
      "name": "remediation-guidance",
      "description": "Provides language-specific remediation guidance, secure code examples, and step-by-step fix instructions for pipeline scan SAST findings. ONLY works with pipeline scan flaw IDs (format: '1234-1' with occurrence suffix). Do NOT call this for platform flaw IDs (plain numeric) or CVE/SID identifiers — it will fail. Already includes flaw details, so calling finding-details first is unnecessary.",
      "category": "veracode",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "flaw_id",
          "type": "string",
          "isRequired": true,
          "description": "Pipeline scan flaw ID (string format with occurrence, e.g., \"1026-1\", \"1026-2\", \"999-1\"). Copy the exact flaw ID from pipeline-findings output."
        }
      ]
    },
    {
      "name": "sca-findings",
      "description": "Get third-party library vulnerabilities from platform-based SCA scans on the Veracode platform (vulnerable dependencies, outdated libraries, CVEs). These are findings from scans already completed on the platform. Do not use for local SCA scan results — use local-sca-findings for those.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "app_profile",
          "type": "string",
          "isRequired": false,
          "description": "Application name (auto-detected if not provided)"
        },
        {
          "name": "severity",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Exact severity (0=Info, 1=VeryLow, 2=Low, 3=Med, 4=High, 5=VeryHigh)"
        },
        {
          "name": "severity_gte",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Minimum severity (0-5)"
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        }
      ]
    },
    {
      "name": "package-workspace",
      "description": "Packages source code into a ZIP artifact suitable for Veracode pipeline scanning. Must be called before pipeline-scan. Stores the artifact in .veracode/packaging/ within the workspace. If a filename is not specified, pipeline-scan will automatically locate the artifact.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "filename",
          "type": "string",
          "isRequired": false,
          "description": "Output filename (saved to .veracode/packaging if no path specified)"
        }
      ]
    },
    {
      "name": "pipeline-scan",
      "description": "Performs a local SAST scan using the Veracode Pipeline Scanner. Requires package-workspace to have been called first. By default runs asynchronously (returns immediately); use pipeline-status to poll for completion and pipeline-findings to retrieve results. Set synchronous=true when results are needed before proceeding, such as in an automated remediation workflow — then call pipeline-findings immediately after.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "app_profile",
          "type": "string",
          "isRequired": false,
          "description": "Application name (auto-detected if not provided)"
        },
        {
          "name": "filename",
          "type": "string",
          "isRequired": false,
          "description": "File to scan (defaults to largest non-log file in .veracode/packaging)"
        },
        {
          "name": "synchronous",
          "type": "boolean",
          "isRequired": false,
          "description": "Wait for the scan to complete before returning. Default is false (scan runs in background). Set to true when the caller needs results before proceeding, such as in an automated pipeline."
        }
      ]
    },
    {
      "name": "pipeline-status",
      "description": "Checks the status of a local pipeline scan (RUNNING, COMPLETED, or no scan found). Call pipeline-findings once COMPLETED is returned. Also indicates whether a previous completed results file exists even when no scan is active.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        }
      ]
    },
    {
      "name": "pipeline-findings",
      "description": "Retrieves findings from a completed local pipeline SAST scan with an interactive UI. Requires a scan to have completed — check pipeline-status first if unsure. By default returns only policy-violating findings. Use finding-details for the full data-flow path on a specific flaw, or remediation-guidance to get fix instructions for a pipeline flaw ID.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "violates_policy",
          "type": "boolean",
          "isRequired": false,
          "description": "Filter by policy violation status. Default is true (policy violations only). Only set this parameter if the user explicitly asks to see all findings regardless of policy. Do not set it to false unless directly requested."
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        }
      ]
    },
    {
      "name": "local-sca-scan",
      "description": "Runs a local SCA scan to identify vulnerable third-party dependencies and IaC misconfigurations. Stores results in the workspace. After completion, call local-sca-findings to retrieve dependency vulnerabilities and local-iac-findings to retrieve IaC misconfigurations — both read from the same scan output.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        }
      ]
    },
    {
      "name": "local-sca-findings",
      "description": "Retrieves vulnerability details from a completed local SCA scan, including CVE identifiers, CVSS scores, EPSS scores, affected versions, and fix version recommendations. Requires a prior local-sca-scan to have run. EPSS score reflects real-world exploitability probability — prioritize findings with EPSS > 0.1 even for medium CVSS scores. Use the cve filter to look up a specific CVE directly.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "cve",
          "type": "string",
          "isRequired": false,
          "description": "Filter by specific CVE ID (e.g., CVE-2024-1234)"
        },
        {
          "name": "component_name",
          "type": "string",
          "isRequired": false,
          "description": "Filter by library or component name (partial match, case-insensitive)"
        },
        {
          "name": "severity_gte",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Minimum severity (2=Low, 3=Medium, 4=High, 5=Critical)"
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        }
      ]
    },
    {
      "name": "local-iac-findings",
      "description": "Retrieves IaC misconfiguration findings (Dockerfiles, Kubernetes manifests, cloud configs) from the most recent local SCA scan. Results come from the same scan run as local-sca-findings — run local-sca-scan once to populate both. By default returns only failed checks.",
      "params": [
        {
          "name": "application_path",
          "type": "string",
          "isRequired": true,
          "description": "Absolute path to the application workspace"
        },
        {
          "name": "target",
          "type": "string",
          "isRequired": false,
          "description": "Filter by target file path (partial match, case-insensitive, e.g. 'Dockerfile')"
        },
        {
          "name": "check_id",
          "type": "string",
          "isRequired": false,
          "description": "Filter by specific check ID (e.g., DS026)"
        },
        {
          "name": "severity_gte",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 5
          },
          "description": "Minimum severity (2=Low, 3=Medium, 4=High, 5=Critical)"
        },
        {
          "name": "status",
          "type": "string",
          "isRequired": false,
          "description": "Filter by status: 'fail' (default), 'pass', or 'all'.  Only set this parameter if the user explicitly asks to see all findings regardless status."
        },
        {
          "name": "page_size",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 1,
            "max": 500
          },
          "description": "Findings per page (1-500, default 10)"
        },
        {
          "name": "page",
          "type": "number",
          "isRequired": false,
          "validation": {
            "min": 0,
            "max": 500
          },
          "description": "Page number (0-based, default 0)"
        }
      ]
    }
  ]
}
